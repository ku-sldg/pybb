{
  "name": "gear Lean Proof Check (semantic tier)",
  "description": "Runs `lake lean LandingGear/Spec.lean -- --json`: every theorem must still PROVE. The appraiser fails on any error diagnostic or hasSorry warning. The lean toolchain is hashed in the same term, before the invocation (measure-then-use)."
}
