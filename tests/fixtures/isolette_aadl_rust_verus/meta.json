{
  "name": "Isolette Verus Verification (semantic tier)",
  "description": "cargo-verus verify over every contract-bearing isolette crate (report-derived): the generated Verus contracts must PROVE against the implemented behavior. The verus toolchain is hashed in the same term, before the invocations (measure-then-use).",
  "copland": "lseq( bseq( run_command_cargo_verus\u00d77 ), APPR )"
}
