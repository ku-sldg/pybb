{
  "name": "Lean Executable Behavior (exec tier)",
  "description": "Runs the built temp-control binary via `lake exe` on one input vector per GUMBO case; the appraiser compares stdout to the expected command. The lean toolchain is hashed in the same term, before the invocations (measure-then-use)."
}
